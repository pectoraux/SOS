{
  "name": "sos",
  "version": "0.1.0",
  "private": true,
  "description": "SOS — Mission-governed operating system for evolving software systems. W1: Mission / Value / Context Model.",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "verify": "bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
